# packages/mcp-server (Move B receiving dock — placeholder)

Empty on purpose. This directory exists so Move B's filter-repo import
(`git filter-repo --path packages/mcp-server/ ...` from pdpp, see
`EXECUTION-PLAN.md` Phase 1.4/2.2) lands as a clean import onto an
already-registered workspace member, not a simultaneous
scaffolding-plus-content change.

Do not add real code here before Move B executes — this placeholder tracks
the receiving location only. Remove this README when Move B's content
lands.
