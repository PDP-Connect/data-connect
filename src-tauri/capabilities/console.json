{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "console",
  "description": "Capabilities for the embedded console window: app commands.",
  "windows": ["console"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:event:allow-listen",
    "core:event:allow-emit"
  ]
}
